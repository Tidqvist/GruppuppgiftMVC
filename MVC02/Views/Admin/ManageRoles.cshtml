@model IEnumerable<MVC02.Models.ViewModels.UserAndRoles>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-controller="Categories" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
               
            </th>
            <th></th>
        </tr>
    </thead>
    @*<tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <a asp-controller="Admin" asp-action="ManageRoles" asp-route-id="@item.Role">@Html.DisplayFor(modelItem => item.Name)</a>
                </td>
                <td>
               
                </td>
            </tr>
        }
    </tbody>*@
</table>
